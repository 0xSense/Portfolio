export type CardInfo = {
  id: number,
  ProjectTitle: string,
  repoURL: string,
  technologyUsedList: string[];
  projectSummary: string;
};

export type BannerButtons  = {
  id: number,
  title: string,
}